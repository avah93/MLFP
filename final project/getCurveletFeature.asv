function [  ] = getCurveletFeature( image )

addpath('CurveLab-2.1.2\fdct_wrapping_matlab\');

image = rgb2gray(image);
image = imresize(image ,[256 256]);
x = fdct_wrapping(image,0 ,1 ,3 ,16);

len = 

for i=1:size(x,2)
    y = x{i};
    for j=1:size(y,2);
        z = y{j};
        size(z)
    end
end

end

